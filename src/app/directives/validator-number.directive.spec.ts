import { ValidatorNumberDirective } from './validator-number.directive';

describe('ValidatorNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorNumberDirective();
    expect(directive).toBeTruthy();
  });
});
